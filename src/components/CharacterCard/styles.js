import {
    StyleSheet
} from 'react-native';

const styles = StyleSheet.create({
    image: {
        resizeMode: 'cover',
        width: null,
        height: 200
    },
    text: {
        color: 'white'
    }
});

export default styles;